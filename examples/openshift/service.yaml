apiVersion: v1
kind: Service
metadata:
  name: mcp-transform-proxy
  namespace: mcp-transform-proxy
  labels:
    app.kubernetes.io/name: mcp-transform-proxy
spec:
  selector:
    app.kubernetes.io/name: mcp-transform-proxy
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
